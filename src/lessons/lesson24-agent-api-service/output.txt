post http://localhost:3001/api/agent-demo
json:
{"message":"请用 Java 后端工程师能理解的方式解释什么是 AI Agent"}

{
    "success": true,
    "data": {
        "answer": "用 Java 后端工程师的视角来看，**AI Agent 并不是一个替代现有微服务的新技术，而是一种“智能编排层”的架构模式**。它把传统的“被动响应式 API”升级成了“主动感知-规划-执行-反馈”的闭环系统。\n\n下面用你熟悉的 Java 技术栈和架构概念进行结构化拆解：\n\n---\n### 🔑 一、核心组件映射表（传统架构 vs AI Agent）\n\n| AI Agent 组件 | Java 后端等价物 | 职责说明 |\n|--------------|----------------|----------|\n| **大模型 (LLM)** | `@Service` 核心决策层 / 动态规则引擎 | 负责意图理解、任务拆解、路径规划。不是写死逻辑，而是运行时生成决策树。 |\n| **工具/插件 (Tools)** | `RestClient` / `Feign` / `DAO` / 第三方 SDK | Agent 的“手脚”。必须提供**确定性、强契约**的接口（如 OpenAPI），供 LLM 按需调用。 |\n| **记忆/上下文 (Memory)** | `ThreadLocal` + `Redis` + 向量数据库 | 短期记忆 = 对话历史/Token 窗口；长期记忆 = 业务库/Embedding 索引。维持状态连续性。 |\n| **规划与编排 (Orchestrator)** | `Spring Cloud Task` / `Temporal` / `Camunda` | 控制 ReAct 循环、重试策略、超时熔断、状态持久化。相当于 Agent 的“主线程调度器”。 |\n| **目标/提示词 (Goal/Prompt)** | `Controller` 入参校验 + 业务需求文档 | 定义 Agent 要达成的 KPI 和边界约束。Prompt 本质是“可版本化的业务规则配置”。 |\n\n---\n### 🔄 二、运行机制：ReAct 循环（附 Java 伪代码类比）\n\n传统微服务是 `Request → Process → Response`（单次同步调用）。  \nAI Agent 的核心是 **Perceive → Think → Act → Observe → Cycle** 的循环：\n\n```java\n// 传统微服务写法（静态路由）\npublic OrderResult handle(OrderReq req) {\n    validate(req);\n    return orderService.create(req);\n}\n\n// AI Agent 写法（动态循环）\npublic AgentResult execute(AgentTask task) {\n    List<Tool> availableTools = toolRegistry.get(task.domain());\n    MemoryContext ctx = memoryManager.load(task.id());\n    \n    while (!goalReached(ctx) && !maxStepsExceeded()) {\n        // 1. 感知 & 思考：LLM 根据当前上下文决定下一步\n        Plan plan = llm.chat(buildPrompt(task.goal(), ctx.history(), availableTools));\n        \n        // 2. 行动：调用具体 Tool（类似 Feign 调用）\n        ToolCall call = plan.nextAction();\n        Object result = call.execute();\n        \n        // 3. 观察 & 更新记忆：解析结果，追加到上下文\n        ctx.append(call, result);\n        memoryManager.save(ctx);\n    }\n    return buildFinalReport(ctx);\n}\n```\n> 💡 **关键差异**：传统代码是“开发者写死流程”，Agent 是“开发者提供工具+设定目标，由 LLM 在运行时动态编排流程”。\n\n---\n### ⚖️ 三、与传统微服务的本质差异\n\n| 维度 | 传统微服务 | AI Agent |\n|------|-----------|----------|\n| **执行逻辑** | 确定性（if/else、策略模式） | 概率性（LLM 推理，需容错与校验） |\n| **路由方式** | 静态配置（网关路由、注解扫描） | 动态决策（运行时根据语义选择 Tool） |\n| **状态管理** | 倾向无状态（Session/Token 外置） | 强依赖上下文记忆（需设计 TTL 与压缩策略） |\n| **可观测性** | 链路追踪（Sleuth/Zipkin）、指标监控 | 需新增 Prompt 版本管理、Tool 调用链、幻觉拦截日志 |\n| **开发范式** | “实现业务逻辑” | “定义目标 + 封装工具 + 约束边界 + 设计评估集” |\n\n---\n### 🏗️ 四、企业级落地建议（Java 生态）\n\n1. **框架选型**：优先使用成熟开源方案，避免重复造轮子\n   - `Spring AI`（Spring 官方，深度集成 Bean 容器）\n   - `LangChain4j`（社区活跃，支持 Function Calling、Memory、RAG）\n   - `Quarkus AI`（适合云原生/低延迟场景）\n\n2. **工程化关键点**\n   - **Tool 契约化**：每个 `@Tool` 方法必须有清晰的参数描述、返回值结构、错误码规范。LLM 不信任模糊接口。\n   - **记忆生命周期**：短期记忆用滑动窗口+摘要压缩；长期记忆走 Embedding + 向量检索（Milvus/pgvector）。\n   - **安全沙箱**：对 Agent 调用的外部系统做权限隔离、频率限制、敏感操作二次确认（Human-in-the-loop）。\n   - **可观测性**：接入 OpenTelemetry，记录 `prompt_version`、`tool_calls[]`、`token_usage`、`confidence_score`。\n\n3. **典型适用场景**\n   - ✅ 适合：跨系统协同、非结构化输入处理、多步决策、自然语言交互（如智能客服、运维排障、数据探查）\n   - ❌ 不适合：高并发 CRUD、强一致性事务、实时性要求极高的计算（仍应交给传统微服务）\n\n---\n### 📦 五、一句话总结类比\n\n> 如果把传统微服务比作**“自动化流水线”**（固定工序、高吞吐、强一致），  \n> 那么 AI Agent 就是**“带质检员和调度员的柔性车间”**（目标导向、动态排产、容忍试错、持续优化）。\n\n在 Java 后端体系中，Agent 不会取代你的 Spring Boot 应用，而是作为**上层智能编排层**，与你现有的 DAO、Feign、Redis、消息队列无缝协作。建议从“单一目标+3~5个确定性 Tool”的小场景开始试点，逐步沉淀 Prompt 模板、Tool 注册中心与评估指标体系。\n\n如需某个具体场景（如：Agent 对接内部 CRM、自动化报表生成、故障自愈）的 Java 代码骨架或架构图，可提供方向，我为你输出可直接跑通的 Demo 结构。",
        "requestId": "2a422659-c20b-4927-8a80-7794c30ae3b1",
        "durationMs": 24470
    }
}